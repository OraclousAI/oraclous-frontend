'use client';
// The onboarder route (/eurail/chat). Client component — the streaming chat UI.
import { ChatPage } from '../../src/pages/ChatPage.js';

export default function Page() {
  return <ChatPage />;
}
