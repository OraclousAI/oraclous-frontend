'use client';
// The dashboard route (/eurail). Client component — the zones are interactive (filters,
// evidence popovers) and read the static corpus directly.
import { DashboardPage } from '../src/pages/DashboardPage.js';

export default function Page() {
  return <DashboardPage />;
}
